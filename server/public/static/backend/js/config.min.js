let waitConfig = {
    // 命名空间
    namespace: 'waitAdmin',
    // 默认主题
    theme: 'default',
    // 是否隐藏标签菜单
    isTabHidden: false,
    // 是否开启标签记忆
    isTabMemory: false,
    // 是否开启切换刷新
    isTabRefresh: false
}